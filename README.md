# Loan-Approval-Dataset-Preprocessing-for-ML
Preprocessed a loan application dataset to handle missing values, outliers, and categorical data. Balanced classes using SMOTE and split data into training/testing sets for classification, demonstrating end-to-end ML workflow from data cleaning to prediction.
